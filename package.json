{
  "name": "realtime-app",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/ui"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "lint": "echo 'lint not configured yet'",
    "lighthouse:ci": "echo 'lighthouse not configured yet'"
  }
}
