# Design Facts (human ground truth — API cannot supply these)

> Fill in the blanks below. Iris will tag these as `STATED` — the highest
> trust level for human-supplied values. Every blank left unfilled becomes
> an `ASSUMED` token that Iris must surface in docs/01-design-gaps.md.

## Interaction
Hover treatment:      # e.g. "darken 8%"
Focus treatment:      # e.g. "2px brand outline, 2px offset"
Disabled treatment:   # e.g. "38% opacity"
Prototype flows:      # e.g. "row click -> detail drawer from right"

## Semantics (map extracted values to meaning)
Primary action colour: #
Danger / destructive:  #
Page background:       #
Which frame is the canonical desktop layout?
Which frames are OUT of scope?

## Realtime
Screens needing live updates:
Update semantics:      # append / replace / patch-in-place

## Breakpoints
Target breakpoints:    # e.g. 640 / 1024 / 1280
