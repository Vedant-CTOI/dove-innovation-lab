# Contracts — single source of truth
Status header on every file: DRAFT | FROZEN
Only @atlas may freeze or amend a FROZEN contract.

  openapi.yaml          REST surface
  socket-events.md      Socket.io event catalogue
  schema.sql            Canonical DB shape
  design/tokens.json    Design tokens (every value provenance-tagged)
  design/components.md  Component inventory + props
